/**
 * Tipados para `./colors.js`.
 */
export const colors: {
  canvas: string;
  surface: string;
  tabBar: string;
  field: string;
  fieldDark: string;
  fieldPlaceholder: string;
  text: string;
  textMuted: string;
  textSoft: string;
  textOnAccent: string;
  button: string;
  accent: string;
  accentSoft: string;
  today: string;
};

export type Colors = typeof colors;
